---
kind: command
id: 06ae979d-86b0-5df1-83aa-c2a58a2ec678
name: jump@1
gist: Pick up a packed dive -- hydrate its scoped repos at their pinned refs and reapply every linked patch chain on top.
scopes: []
meta:
  usage: nosedive jump
  agents-use-when: the workspace is cold (or a packed dive needs picking back up) and you need scoped repos hydrated with prior WIP reapplied before starting work.
  adapter: kb/artifacts/019fcfbe-f654-7171-96d5-665e3bb4ce94.mjs
  entrypoint: L1__jump
---

# Jump

Requires an active dive marker at `<workspace>/.nosedive-ref`. For every repo
scoped on that dive, hydrates a worktree detached at the scope's pinned `ref`
(creating it under the managed cache if it doesn't exist yet, same as
`hydrate-repo.workspace`).

Then, for every patch chain the dive links with `rel: patch`, walks it
oldest-to-newest via each memo's `rel: next` and reapplies in order:

- A `<sha12>.<slug>` memo is a real commit: applied with `git am`, so the
  original sha, author, and message survive when the parent tree still
  matches.
- A `dirty.<slug>` or `bridge-wip.<hash>` memo is a working-tree diff: applied
  with `git apply --binary`, left uncommitted.

`<slug>` matches a chain to the scoped repo whose kb `kind: repo` doc has that
`name`; a `bridge-wip.*` chain applies straight onto the bridge's `kb/` tree
instead, and is deliberately left uncommitted -- bridge content changes only
land when a human commits or runs `land`.

## Bookkeeping

Once every chain applies cleanly, the applied memo docs and `.patch` artifact
files are deleted and their `rel: patch` links removed from the dive doc.
`meta.diver` is set to `"<pilot> picked up <effort-slug>"`, and the bridge
commits that message -- staging only the dive doc and the now-deleted
artifact files, same fetch + `merge --ff-only` + push discipline as `pack`.
`.nosedive-ref` is (re)written last.

A dive with no `rel: patch` links still hydrates every scoped repo at its
pinned ref; it just has nothing to reapply.

---
kind: command
id: 0a4fe90c-9f9a-57e4-8234-750660f174d4
name: pack@1
gist: Capture all WIP on the active dive's scoped repos and bridge kb/ as patch artifacts, commit and push the bridge, then force-dehydrate every scoped repo.
scopes: []
meta:
  usage: nosedive pack
  agents-use-when: a work session is ending, or a dive needs to be handed off, and its workspace WIP should be preserved before the workspace is torn down.
  adapter: kb/artifacts/019fcefe-4bdc-731b-9fe9-0fafb72fa35a.mjs
  entrypoint: L1__pack
---

# Pack

Requires an active dive marker at `<workspace>/.nosedive-ref`. For every repo
scoped on that dive:

- A writable scope is captured: one binary-safe patch artifact per commit
  ahead of the scope's pinned `ref` (`git format-patch`, preserving the
  original sha and subject), then one final patch for any remaining
  staged/unstaged/untracked changes (`git diff --binary HEAD`, untracked files
  folded in via intent-to-add).
- A read-only scope is never packed. If it carries any commits ahead of its
  pin or any dirty state, `pack` refuses rather than discard it silently;
  re-scope it writable or dehydrate it by hand first.

Any other dirty file under the bridge's `kb/` directory (besides the dive
doc itself and the artifacts just written) is captured the same way, as one
combined patch.

Every artifact is linked on the dive doc's `links:`, in the order captured, so
a later reapply is deterministic: `rel: wip-patch` entries carry `repo`, and
either `sha` + `message` (a real commit) or `dirty: true` (working-tree
state); the bridge-wide patch, if any, links `rel: bridge-wip`.

## Commit and push

Only the dive doc and the new artifact files are staged. Anything else dirty
in the bridge is stashed first (`git stash push --keep-index`) so the commit
and the fast-forward pull that precedes it see a clean tree, then popped back
after the push. The commit message is `dive(<dive-name>): packed wip`. The
bridge must have an upstream to push to; pack refuses otherwise. A failed
fast-forward pull or push fails loud -- the dive is left committed locally.

## Teardown

Once the bridge push (or the no-op case, when there was nothing to pack)
completes, every scoped repo -- writable and read-only alike -- is force
dehydrated (`git worktree remove --force`), same as `dehydrate-repo.workspace
--force`.

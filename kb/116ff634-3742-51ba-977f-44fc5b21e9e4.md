---
kind: command
id: 116ff634-3742-51ba-977f-44fc5b21e9e4
name: list-dives@2
gist: Print pickupable and working dives for an open feat, read from the feat's durable `links:` frontmatter.
scopes: []
meta:
  use-instead: "`preflight`, which shows the backlog and every free dive without naming a feat."
  usage: nosedive list-dives <feat> [--include-historical] [--json]
  agents-use-when: you need to know which dives on a feat are pickupable or already held.
  adapter: kb/artifacts/019fda66-c78d-7a4b-8cc4-6bc726ae80c1.mjs
  entrypoint: L2__listDives
links:
  - kb/ad3bc6d7-d4cd-5381-a98f-cb13f9a801d6.md:
      rel: supersedes
---

# List dives (deprecated)

Use `preflight` instead. Its backlog and free-dive surface is the supported way
to find work. This command remains available for compatibility with older
bridges.

`<effort>` accepts a feat path, feat directory path, or leaf-first slug
chain.

Dives are read from the feat's `links:` frontmatter -- the same durable link
list kb docs use. A link is a repo-root file path such as `kb/<id>.md` or a
`- kb/<id>.md: { rel, anchor }` object; only links resolving to `kind: dive`
docs are considered.

## Sections

- **Pickupable** -- linked dives with `rel: pending`.
- **Working** -- linked dives with `rel: working` or `rel: reviewing`, or any
  linked dive with `meta.diver` set.
- **Historical** -- linked dives with no pickup role, plus any dive whose
  `meta.effort` resolves to the feat. Shown only with
  `--include-historical`.

## Warnings

Broken dive links (missing from kb, or pointing at another feat) and held
dives that name the feat but are not linked from it are reported as
warnings.

`--json` prints the same sections as structured data for agent workflows.

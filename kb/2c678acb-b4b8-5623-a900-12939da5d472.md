---
kind: command
id: 2c678acb-b4b8-5623-a900-12939da5d472
name: record.gate@2
gist: Mint a test gate -- its doc, its stub script, and the link declaring it on a feat -- ready to fail until somebody writes the check.
scopes: []
meta:
  usage: nosedive record.gate "<gist>" --feat <feat-ref> [--name <slug>] [--height <n>] [--flaky] [--action <test|land>]
  agents-use-when: the pilot wants a check that outlives the dive that prompted it, and no gate doc exists for it yet.
  adapter: kb/artifacts/01a00db3-9b4d-712b-88e9-39e715350f07.mjs
  entrypoint: L2__gate_record
---

# Record gate

The gist is the first positional argument, the one thing a gate cannot be
useful without, matching `pitch`. `--feat` is required and everything else has
a workable default.

## The doc and the script are one thing

`record.gate` writes both: a `kind: gate` doc carrying
`meta.test-script`, and the script it names at `kb/artifacts/<gate-id>.mjs`.

Minting only the doc would produce something that hard-fails the moment
anything selects it. A gate whose `meta.test-script` is missing or resolves to
no file is a hard error rather than a skip, deliberately -- silently skipping a
broken gate turns it into a passing land -- so the two are never written apart.

The stub fails. It writes its own id to stderr and returns `false`, which the
gate runner reports as exit 1. An unwritten gate that returned cleanly would
report green having checked nothing, which is the failure the whole area exists
to prevent, so an unwritten gate is a red one and the pilot is told where to
write it.

## What it links

A `rel: test.gate` link on the `--feat` target by default, or `rel: land.gate`
with `--action land`. `--height` sets `gate-height` and `--flaky` sets
`test-is-flaky`, both on that link rather than on the gate doc: one prover can
be a `test.gate` on a feat and a `land.gate` on a repo, and its height differs
per verb. A gate doc never names its own phase.

A `test.gate` is the loop a pilot runs while working -- `land` never collects
it, so it can fail without blocking publication. A `land.gate` is what
publication is conditional on: `land` collects `rel: land.gate` links and
refuses to land while one fails. `--action` defaults to `test`.

`--feat` must resolve to a `kind: feat` doc. A failing gate mints work
against the feat that declared it, so a gate declared anywhere else leaves its
own failure with nowhere to go.

`--name` is the gate's slug in kebab-case, and is what gate reports call it.
Omitted, the gate is named `new-gate-<yyyy>-<mm>-<dd>-<hhmmss>`, so a gate can
be minted before it has a good name and renamed later -- the same default, for
the same reason, as `pitch`.

## No scopes key

The minted doc writes no `scopes:` key at all. An absent one inherits the
scopes of the doc that declared the gate, which is what a new gate wants; an
explicit `scopes: []` says the opposite -- this gate needs no repo -- and would
pin it to nothing.

## Not here

There is no `--repo`. A repo may declare `land.gate` and nothing else, and the
rule that enforces that placement lives in the pre-push hook, which does not
check it yet. The flag ships when the rule does.

---
kind: command
id: 32123800-a61d-5ea1-8b85-98c288b127b3
name: dehydrate-repo.workspace@1
gist: Remove one hydrated workspace checkout for a kb repo without touching managed cache or bridge metadata.
scopes: []
meta:
  usage: nosedive dehydrate-repo.workspace <repo-id-or-name-or-workspace-path> [--force]
  adapter: kb/artifacts/019fadf5-e099-7e9a-bfc0-dad7fd3bf036.mjs
  entrypoint: L1__workspace_dehydrateRepo
---

# Dehydrate repo

The target is required and accepts either:

- a repo `id`
- an exact repo `name`
- a bridge-workspace-relative directory path (or `.nosedive-ref` path) for an
  already hydrated checkout

`--force` bypasses local dirty/unpublished-work protection only.

## Behavior

- The command resolves and validates the configured workspace target for the
  repo and requires the managed `.nosedive-ref` ownership marker before
  removing anything.
- Target paths must remain inside configured `workspace:` and cannot be
  widened by `--force`.
- Without `--force`, dehydration refuses to remove a checkout with uncommitted
  changes or unpublished commits.
- Success status is always one of `removed` or `noop`.

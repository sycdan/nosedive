---
kind: command
id: 3570e756-f8e7-5e95-b911-09d7d116cd23
name: nuke@1
gist: Remove nosedive-managed bridge config files or force-remove managed workspace worktrees; refuses to run without a destructive switch.
scopes: []
meta:
  usage: nosedive nuke --config|--workspace
  agents-use-when: only on explicit pilot request.
  adapter: kb/artifacts/019fadf5-e09d-7231-9672-186e4ec4f334.mjs
  entrypoint: L1__nuke
---

# Nuke

Destructive, so it fails unless `--config` or `--workspace` is provided.

With `--config`, removes `.nosediverc`, `.nosedive/config.yaml`,
`.nosedive.local.yaml`, and `.nosedive/.gitignore` when present, and reports
how many files it removed.

Also removes nosedive-managed config exclude blocks from Git's local
`.git/info/exclude`, including the older package-foundation exclude block.

With `--workspace`, force-removes managed Git worktrees, verifies their source
repos no longer register a worktree at that path, removes
`<nosedive-workspace>/.nosedive-ref`, and removes [the workspace scratch
space](019feeeb-ff29-720d-ad65-0c77a2957744.md). Other hidden directories and
unmanaged workspace entries are left alone.

Bridge kb and backlog contents are left alone.

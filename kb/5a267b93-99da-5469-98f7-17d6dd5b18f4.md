---
kind: command
id: 5a267b93-99da-5469-98f7-17d6dd5b18f4
name: land@1
gist: Canonicalize the active dive -- push every writable scoped repo to work-branch-prefix<slug>, close the dive as a memo with an outcome section, and leave each scoped worktree hydrated at the commit it pushed.
scopes: []
meta:
  usage: nosedive land
  agents-use-when: the active dive's work is ready to publish for real, not just packed as WIP.
  adapter: kb/artifacts/019fd254-5b14-75e7-a72e-4bab42064a40.mjs
  entrypoint: L1__land
---

# Land

Guard: same `no dive, run nosedive into "<something>"` as `jump`/`pack`/`bail`.

For every scoped repo with `mode: rw` that is currently hydrated, pushes its
worktree's HEAD to `<work-branch-prefix><effort-slug-or-dive-name>` on that
repo's own `origin` (already pointed at the real cloud remote by hydration --
read-only scopes are skipped, never pushed). Refuses instead of silently
skipping if a scope's hydration state can't be determined.

Then: `kind` flips `dive` -> `memo`, a `## Outcome` section is appended to the
body (dive gist + one line per pushed repo/branch), and the change is
committed and pushed to the bridge using the same stash-except-staged /
fetch+ff-only-merge sequence `pack`/`bail` use. No worktree is removed: every
scoped repo stays hydrated where it stands, detached at the commit that was
just pushed, with its `.nosedive-ref` marker intact. Clearing it is a separate,
deliberate act -- `dehydrate-repo.workspace` or `nuke --workspace`. The dive
marker is removed last, after the bridge push succeeds.

A landed dive is final -- `kind: memo` docs are not picked back up by `into`.

---
kind: command
id: 88bf4e10-3fd7-58c0-a247-f2dae9c886e3
name: scan@1
gist: "Hydrate one repo and print a documentation-only brief for recording its workloads, quality gates, and conventions."
scopes: []
meta:
  usage: nosedive scan --repo <repo-id-or-name>
  agents-use-when: you need a sourced repo-doc record of documented workloads, quality gates, and local conventions.
  adapter: kb/artifacts/019fd7c4-98e8-733e-b9e7-41a7622dfcaa.mjs
  entrypoint: L1__scan
---

# Scan

`--repo <repo-id-or-name>` is required exactly once and resolves a `kind: repo`
doc by UUID or exact name. The managed workspace checkout is hydrated before the
brief is printed.

The brief directs a later agent to inspect documentation surfaces only: README,
contributing guides, `docs/`, hooks, and CI configuration. It must not inspect
source code or guess. The agent records sourced quality gates and conventions on
documented runnable workload, recording documented run instructions in its body.

On rescans, matching loads are updated in place so their IDs, inbound links, and
unrelated hand edits remain intact. `scan` without `--repo` and `--deep` are not
implemented and fail clearly.

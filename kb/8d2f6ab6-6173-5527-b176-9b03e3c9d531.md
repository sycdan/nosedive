---
kind: command
id: 8d2f6ab6-6173-5527-b176-9b03e3c9d531
name: test@1
gist: Run the active dive's gates, streaming each one's output as it runs, and exit 1 if any failed.
scopes: []
meta:
  usage: nosedive test [land | <gate-id>...]
  agents-use-when: you want to know whether the work so far holds up, at any point during a dive.
  adapter: kb/artifacts/8d2f6ab6-6173-5527-b176-9b03e3c9d531.mjs
  entrypoint: L1__test
links:
  - kb/019fe2f7-5922-72d5-abda-b5b8cb7300cf.md:
      rel: throws
---

# Test

Runs gates and reports what held. Every selected gate runs, in sequence, each
in its own process; output streams as it is produced rather than after the run.

## Selecting gates

With no arguments, runs the gates the active dive claims through its own
`rel: land.gate` links, and nothing else. That is the form to reach for while
working: the checks for what is being changed, not everything that guards the
repository.

`land` widens the selection to every gate reachable from the dive, its feat and
its scoped repos -- exactly the set `land` would run, so a clean `test land` is
a truthful preview of a land rather than a similar one. It does not land
anything, and runs nothing `land` would not.

`<gate-id>...` runs the named gates by quid, whatever they are attached to, in
the order given.

Every argument names *what* to test. `land` is a set the same way a quid is a
gate, which is why it is a bare word rather than a flag -- `--land` on a command
that runs gates reads as an instruction to land afterwards. Gates resolve by
quid only, so the word can never be mistaken for one. Listing `land` alongside a
gate is refused: `land` already includes every gate a land would run.

A dive gate needs no `gate-height`. Height exists to sequence a land; a
dive-sized set is small enough that discovery order is the honest order.

## Requires a dive

Every form except named gates needs a dive on deck, and errors with
`no-active-dive.error` without one. There is nothing to select from otherwise:
the selection roots *are* the dive, its feat and its scoped repos.

Naming gates is the exception, because they are already chosen.

## Reporting

A failing gate does not stop the run. Every selected gate runs, every failure
is named at the end, and the command exits 1 -- "these three are broken" is
actionable where "something is broken" only earns another run.

A gate document must have a gate kind and a bridge-relative `meta.test-script`
exporting `run(ctx)`. Hydrated workspace repositories are provided through
`ctx.repos`, and the active dive id through `ctx.diveId`.

A dive that links no gates is reported and exits non-zero. Zero gates is the
one verdict a test command must never call success: it would teach a pilot that
green means checked when it means unchecked.

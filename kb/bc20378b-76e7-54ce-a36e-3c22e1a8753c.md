---
kind: command
id: bc20378b-76e7-54ce-a36e-3c22e1a8753c
name: add-repo.effort@1
gist: "Add a kb `kind: repo` doc to the active effort's `scopes:` by resolving the repo by id or exact name."
scopes: []
meta:
  usage: nosedive add-repo.effort <repo-id-or-name> [--ref <ref>] [--read-only]
  agents-use-when: "the effort you are working under needs another repo in its `scopes:`."
  adapter: kb/artifacts/019fbebf-2b09-75a5-9fc7-fa6b0c6f64e3.mjs
  entrypoint: L1__effort_addRepo
---

# Add repo to effort

Resolves `<repo-id-or-name>` against bridge KB `kind: repo` docs by `id` or
exact `name`, then appends that repo id to the active `kind: feat` doc's
`scopes:` list.

The active effort is read from local `current.effort`, or from the active dive
marker when a dive is selected. `current.effort` may be an effort doc quid,
bridge-root KB path such as `kb/<id>.md`, or exact effort `name`.

- `--ref` pins the effort scope to a ref. When omitted, the scope has no `ref`
  key and downstream scope resolution falls back to the repo's configured trunk.
- `--read-only` is what a scope naming no branch already is, so it still parses
  and records nothing. `mode:` is no longer written at either level; see
  [add-repo.feat@2](16b4fee2-cc6c-5c29-9327-fde39a35b0a8.md).

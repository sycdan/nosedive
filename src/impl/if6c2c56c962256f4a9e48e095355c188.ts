// `add-repo.effort` is the superseded spelling of `add-repo.feat` at both
// levels, so both route to the current command's implementation rather than
// keeping a second copy that could drift from it.
export { run } from "./i61b2a0b9891f53cbbf99667ca8be6b3f.js";
